/**
 * Cannon Component - Renders the cannon at bottom of screen
 * Note: Cannon is currently drawn directly in Game.jsx canvas
 * This file is kept for modular structure and future expansion
 */

const Cannon = ({ x, y, angle }) => {
  // Cannon logic is integrated in Game.jsx for canvas drawing
  // This component serves as reference for cannon properties
  return null;
};

export default Cannon;
