/**
 * Bullet Component - Represents projectiles
 * Note: Bullet rendering is done on canvas in Game.jsx
 * This file is kept for modular structure and future expansion
 */

const Bullet = ({ bullet }) => {
  // Bullet logic is integrated in Game.jsx for canvas drawing
  // This component serves as reference for bullet properties
  return null;
};

export default Bullet;
